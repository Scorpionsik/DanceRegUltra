<window:DialogWindowExt x:Class="DanceRegUltra.Views.SchemeManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:window="clr-namespace:CoreWPF.Windows;assembly=CoreWPF"
        xmlns:converter="clr-namespace:DanceRegUltra.Utilites.Converters"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"     
        xmlns:corewpf="clr-namespace:CoreWPF.Utilites;assembly=CoreWPF"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" Height="450" Width="800">
    <window:DialogWindowExt.Resources>
        <converter:CategoryNameByIdConvert x:Key="CategoryName"/>
    </window:DialogWindowExt.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <!--Схемы-->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <!--Таблица-->
                <ListBox Grid.Row="0" ItemsSource="{Binding Path=Schemes}" SelectedItem="{Binding Path=Select_scheme}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Title_scheme}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!--конец Таблица-->
                <!--Кнопки-->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="5"/>
                        </Style>
                    </Grid.Resources>
                    <Button Grid.Column="0" Content="Add" Command="{Binding Path=Command_AddNewScheme}"/>
                    <Button Grid.Column="1" Content="Delete" Command="{Binding Path=Command_DeleteSelectScheme}" CommandParameter="{Binding Path=Select_scheme}"/>
                </Grid>
                <!--конец Кнопки-->
            </Grid>
            <!--конец Схемы-->
            <!--Выбранная схема-->
            <Grid Grid.Column="1" DataContext="{Binding Path=Select_scheme}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <GroupBox Grid.Row="0" Header="Name">
                    <TextBox Text="{Binding Path=Title_scheme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </GroupBox>
                <TabControl Grid.Row="1">
                    <TabItem>
                        <TabItem.Header>
                            <TextBlock Text="{Binding StringFormat=Платформы: {0}, Path=PlatformsCollection.Count}"/>
                        </TabItem.Header>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <!--Таблица платформ-->
                            <ListBox 
                                dd:DragDrop.IsDragSource="True"
                                dd:DragDrop.IsDropTarget="True"
                                Grid.Row="0" 
                                BorderThickness="0" 
                                VirtualizingPanel.ScrollUnit="Pixel"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ItemsSource="{Binding Path=PlatformsCollection}"
                                SelectedItem="{Binding Path=Select_platform}"
                                >
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="DarkGray"/>
                                        <Setter Property="Margin" Value="5 2.5 5 2.5"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel LastChildFill="True">
                                            <GroupBox DockPanel.Dock="Top" Header="Name">
                                                <TextBox Text="{Binding Path=TitleSchemePart, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                            </GroupBox>
                                            <GroupBox Header="Выбранные лиги">
                                                <!--Таблица лиг-->
                                                <xctk:CheckListBox
                                                   dd:DragDrop.IsDragSource="True"
                                                   dd:DragDrop.IsDropTarget="True"
                                                   dd:DragDrop.DropHandler="{Binding}"
                                                   SelectedMemberPath="IsChecked"
                                                   BorderThickness="0"
                                                   ItemsSource="{Binding Path=SchemePartValues}"
                                                   ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                                    <i:Interaction.Behaviors>
                                                        <corewpf:IgnoreMouseWheelBehavior />
                                                    </i:Interaction.Behaviors>
                                                    <xctk:CheckListBox.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </xctk:CheckListBox.ItemsPanel>

                                                    <xctk:CheckListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding Path=Id, Converter={StaticResource CategoryName}, ConverterParameter=league}"/>
                                                        </DataTemplate>
                                                    </xctk:CheckListBox.ItemTemplate>
                                                </xctk:CheckListBox>
                                                <!--конец Таблица лиг-->
                                            </GroupBox>
                                        </DockPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <!--конец Таблица платформ-->
                            <!--Кнопки платформ-->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.Resources>
                                    <Style TargetType="Button">
                                        <Setter Property="Margin" Value="5"/>
                                    </Style>
                                </Grid.Resources>
                                <Button Grid.Column="0" Content="Add" Command="{Binding Path=Command_AddPlatform}"/>
                                <Button Grid.Column="1" Content="Remove" Command="{Binding Path=Command_DeletePlatform}" CommandParameter="{Binding Path=Select_platform}"/>
                            </Grid>
                            <!--конец Кнопки платформ-->
                        </Grid>
                    </TabItem>
                    <TabItem>
                        <TabItem.Header>
                            <TextBlock Text="{Binding StringFormat=Блоки: {0}, Path=BlocksCollection.Count}"/>
                        </TabItem.Header>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <!--Таблица блоков-->
                            <ListBox 
                                dd:DragDrop.IsDragSource="True"
                                dd:DragDrop.IsDropTarget="True"
                                Grid.Row="0" 
                                BorderThickness="0" 
                                VirtualizingPanel.ScrollUnit="Pixel"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ItemsSource="{Binding Path=BlocksCollection}"
                                SelectedItem="{Binding Path=Select_block}"
                                >
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="DarkGray"/>
                                        <Setter Property="Margin" Value="5 2.5 5 2.5"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel LastChildFill="True">
                                            <GroupBox DockPanel.Dock="Top" Header="Name">
                                                <TextBox Text="{Binding Path=TitleSchemePart, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                            </GroupBox>
                                            <GroupBox Header="Выбранные возрастные категории">
                                                <!--Таблица возрастн категорий-->
                                                <xctk:CheckListBox 
                                                   BorderThickness="0"
                                                   dd:DragDrop.IsDragSource="True"
                                                   dd:DragDrop.IsDropTarget="True"
                                                   dd:DragDrop.DropHandler="{Binding}"
                                                   SelectedMemberPath="IsChecked"
                                                   ItemsSource="{Binding Path=SchemePartValues}"
                                                   ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                                    <i:Interaction.Behaviors>
                                                        <corewpf:IgnoreMouseWheelBehavior />
                                                    </i:Interaction.Behaviors>
                                                    <xctk:CheckListBox.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </xctk:CheckListBox.ItemsPanel>
                                                    <xctk:CheckListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding Path=Id, Converter={StaticResource CategoryName}, ConverterParameter=age}"/>
                                                        </DataTemplate>
                                                    </xctk:CheckListBox.ItemTemplate>
                                                </xctk:CheckListBox>
                                                <!--конец Таблица возрастн категорий-->
                                            </GroupBox>
                                        </DockPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <!--конец Таблица блоков-->
                            <!--Кнопки блоков-->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.Resources>
                                    <Style TargetType="Button">
                                        <Setter Property="Margin" Value="5"/>
                                    </Style>
                                </Grid.Resources>
                                <Button Grid.Column="0" Content="Add" Command="{Binding Path=Command_AddBlock}"/>
                                <Button Grid.Column="1" Content="Remove" Command="{Binding Path=Command_DeleteBlock}" CommandParameter="{Binding Path=Select_block}"/>
                            </Grid>
                            <!--конец Кнопки блоков-->
                        </Grid>
                    </TabItem>
                    <TabItem Header="Стили">
                        <GroupBox Header="Выбранные стили">
                            <!--Таблица стилей-->
                            <xctk:CheckListBox 
                                BorderThickness="0"
                                dd:DragDrop.IsDragSource="True"
                                dd:DragDrop.IsDropTarget="True"
                                dd:DragDrop.DropHandler="{Binding}"
                                SelectedMemberPath="IsChecked"
                                ItemsSource="{Binding Path=SchemeStyles}"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <xctk:CheckListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </xctk:CheckListBox.ItemsPanel>
                                <xctk:CheckListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Path=Id, Converter={StaticResource CategoryName}, ConverterParameter=style}"/>
                                    </DataTemplate>
                                </xctk:CheckListBox.ItemTemplate>
                            </xctk:CheckListBox>
                            <!--конец Таблица стилей-->
                        </GroupBox>
                    </TabItem>
                </TabControl>
            </Grid>
            <!--конец Выбранная схема-->
        </Grid>
        <Button Grid.Row="1" Content="Save" Command="{Binding Path=Command_save}"/>
    </Grid>
</window:DialogWindowExt>
